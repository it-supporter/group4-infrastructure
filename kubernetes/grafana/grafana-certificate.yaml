apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-pulseai
  namespace: portal
spec:
  secretName: grafana-pulseai-tls

  issuerRef:
    name: pulseai-ca
    kind: ClusterIssuer

  dnsNames:
    - grafana.gruppe4.local
