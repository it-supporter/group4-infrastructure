
name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [host]

    steps:
      - name: Show environment
        run: |
          whoami || true
          hostname || true
          pwd || true

      - name: Inspect host runner context
        run: |
          echo "===== USER ====="
          whoami || true

          echo "===== ID ====="
          id || true

          echo "===== HOSTNAME ====="
          hostname || true

          echo "===== CURRENT DIRECTORY ====="
          pwd || true

          echo "===== CYBERRANGE REPO ====="
          ls -la /home/henrik/git/CyberRange | head || true

          echo "===== TERRAFORM ====="
          which terraform || true

          echo "===== ANSIBLE ====="
          which ansible-playbook || true

          echo "===== STATUS API ====="
          ls -ld /home/henrik/cyberrange-api || true

      - name: Inspect runner filesystem
        run: |
          echo "===== ROOT ====="
          ls -la /

          echo "===== HOME ====="
          echo "$HOME"

          echo "===== MOUNTS ====="
          mount | head -50 || true

          echo "===== PASSWD ====="
          cat /etc/passwd || true

      - name: Inspect /data mount
        run: |
          echo "===== DATA ====="
          ls -la /data

          echo "===== DATA RECURSIVE ====="
          find /data -maxdepth 3 -type f | sort
